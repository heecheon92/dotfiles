# LazyVim configuration

This is the Home Manager-managed editor configuration for these dotfiles.
It starts from LazyVim's default keybindings and adds the selected language extras,
explicit Oil/completion/search key preferences, and small local overrides.

See the repository's [editor guide](../../../docs/editor.md) for tool ownership,
notebook commands, verification results, and known limitations.
